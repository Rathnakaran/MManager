
import CalendarClient from '@/components/pages/calendar/calendar-client';

export default async function CalendarPage() {
  // This page is client-side rendered to access localStorage
  return <CalendarClient />;
}
